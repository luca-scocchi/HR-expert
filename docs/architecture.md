## Workflow Architecture

The workflow is composed of:
- Trigger nodes (input)
- Processing nodes (logic + transformation)
- AI nodes (analysis / generation)
- Output nodes (final structured result)

Each block is isolated to reduce errors and allow easy debugging.
